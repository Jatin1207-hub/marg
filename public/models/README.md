This directory is the drop zone for .glb shoe model files.

Name each file using the product ID from products.ts, e.g.:
  sv-velocity-x.glb
  sv-aero-pulse.glb
  etc.

The Shoe3D component will automatically load these files when available,
falling back to the procedural shoe geometry when a file is missing.
